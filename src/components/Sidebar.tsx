import { Box } from '@mui/material'
import React from 'react'

type Props = {}

const Sidebar = (props: Props) => {
  return (
    <Box sx={{backgroundColor: 'blue'}}>Sidebar</Box>
  )
}

export default Sidebar